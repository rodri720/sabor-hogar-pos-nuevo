// server/src/services/printService.js
// Versión desactivada (no requiere node-thermal-printer ni printer)
export async function printTicket(ticketData) {
    console.log('🖨️ Impresión desactivada temporalmente. Ticket no enviado a la impresora.');
    // No hace nada, solo evita errores de módulos faltantes.
}