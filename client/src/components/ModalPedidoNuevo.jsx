import { useState, useEffect } from 'react';
import { Modal, Button, Row, Col, ListGroup, Form, Alert, Spinner } from 'react-bootstrap';
import { getProductos, getCombos, crearVenta, cerrarVenta } from '../api';

export default function ModalPedidoNuevo({ show, onHide, mesa, mozos, onPedidoCreado }) {
  // Copia aquí TODO el código interno de ModalPedido (sin cambiar el nombre del componente)
  // O usa la versión completa que te dí antes, pero con el nombre ModalPedidoNuevo
  return <div>Prueba</div>; // temporal
}
